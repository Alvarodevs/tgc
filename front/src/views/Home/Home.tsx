import { Link } from 'react-router-dom'
import Card from '../../components/Card'

interface Props {}

const Home = (props: Props) => {
  return (
    <div>Home
      <Card/>
    </div>
  )
}

export default Home